# pi-sys-monitor
A Pi system monitor: A command-line tool in C that reads CPU usage, memory, and temperature from /proc and /sys on the Pi, and displays them in a live-updating terminal dashboard. Exposed via Cloudflare tunnel.

As this is a project to learn more about shells, no AI coding tools were used for code generation.
